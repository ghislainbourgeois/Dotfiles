#!/bin/bash

git_dir=~/Dotfiles
home=~/

ln -sf $git_dir/.bashrc $home/.bashrc
ln -sf $git_dir/.tmux.conf $home/.tmux.conf
ln -sf $git_dir/.vimrc $home/.vimrc
ln -sf $git_dir/.vim $home/.vim
ln -sf $git_dit/.i3 $home/.i3
 
